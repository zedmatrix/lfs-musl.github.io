# New Revision - Musl Linux From Scratch

1. Check the `prepare` section for filesystem preparation

2. The `crosstools` section is the combined Chapters 5, 6 and 7

3. The `base-system` section is the main musl base build and rebuild of packages

4. The `system-config` section, finishes the preparation for kernel building

5. The `kernel-build` section finalizes the kernel and prepares to reboot.
