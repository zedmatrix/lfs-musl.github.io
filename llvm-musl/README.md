# Preparing LLVM - Musl

First prepare the cross tools from this point you can continue with Binutils, GCC, Musl <br>
or build a standalone static llvm/clang to remove the Binutils and GCC from the build <br>
system.

## Preparing Cross Tools
<br>
Prepare LFS upto chapter 4 where the environment is slightly different from ` 0-environment.sh `

<br>Source File SHA256 Sums: ` sources.sha256 `
<br>wget List: ` sources.wget `
